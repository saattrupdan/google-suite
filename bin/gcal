#!/usr/bin/env bash
# The old name. This app is Mail and Calendar now, not just Calendar,
# so the command is gsuite; this stays so muscle memory and old aliases work.
exec "$(cd "$(dirname "$0")" && pwd)/gsuite" "$@"
